[pytest]
# Restrict test discovery to a dedicated `tests/` folder so top-level
# script-like files named `test_*.py` are not imported by pytest during
# collection (they are intended to be runnable scripts, not pytest tests).
testpaths = tests
python_files = test_*.py
# Default options: disable capture to avoid interaction issues and run quietly
addopts = -s -q
